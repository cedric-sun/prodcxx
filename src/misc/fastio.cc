#include <bits/stdc++.h>
#include <stdio_ext.h>

using namespace std;

auto fasterio = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    __fsetlocking(stdin, FSETLOCKING_BYCALLER);
    __fsetlocking(stdout, FSETLOCKING_BYCALLER);
    static char stdinbuf[1024*1024];
    static char stdoutbuf[1024*1024];
    setbuffer(stdin,stdinbuf,sizeof(stdinbuf));
    setbuffer(stdout,stdinbuf,sizeof(stdoutbuf));
    return nullptr;
}();

auto fastio = []() {
    using namespace std;
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return nullptr;
}();


int main() {
	return 0;
}
